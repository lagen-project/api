server:
    host: http://api.lagen.local
    port: 80
    username: pierre
    password: plop
project_slug: lagen-api
root_dir: features
install: "composer install && mkdir -p tests/data && php bin/console doctrine:database:create --env=test && php bin/console doctrine:schema:update --force --env=test"
test: "./vendor/bin/behat --format=progress"
