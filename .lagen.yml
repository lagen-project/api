server:
    host: http://api.lagen.local
    port: 80
    username: pierre
    password: plop
project_slug: lagen-api
root_dir: features
install:
    - "composer install"
    - "mkdir -p tests/data var/jwt"
    - "php bin/console doctrine:database:create --env=test"
    - "php bin/console doctrine:schema:update --force --env=test"
    - "openssl genrsa -out config/jwt/private.pem -passout pass:lagen"
    - "openssl rsa -pubout -in config/jwt/private.pem -out config/jwt/public.pem -passin pass:lagen"
test: "./vendor/bin/behat --format=progress"
