services:
    app.manager.feature:
        class: AppBundle\Manager\FeatureManager
        arguments:
            - "@filesystem"
            - "%projects_root_dir%"
            - "@slugify"
            - "@app.parser.feature"
    app.manager.project:
        class: AppBundle\Manager\ProjectManager
        arguments:
            - "@filesystem"
            - "%projects_root_dir%"
            - "@slugify"
            - "@app.parser.feature"
